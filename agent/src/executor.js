/**
 * Motor de execução paper-trade — Passo 7 (stub)
 * Simula ordens localmente antes de ligar à exchange real.
 */

// TODO Passo 7: implementar openPosition(signal, size, state) e closePosition(id, price, state)
export async function openPosition(_signal, _size, _state) {
  throw new Error("executor.js não implementado — ver Passo 7 em strategy.md");
}

export async function closePosition(_positionId, _price, _state) {
  throw new Error("executor.js não implementado — ver Passo 7 em strategy.md");
}
