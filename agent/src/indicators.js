/**
 * Motor de indicadores técnicos — Passo 4 (stub)
 * EMA, RSI, Volume MA calculados sobre arrays de velas OHLCV.
 */

// TODO Passo 4: implementar ema(values, period), rsi(closes, period), volumeMA(volumes, period)
export function ema(_values, _period) {
  throw new Error("indicators.js não implementado — ver Passo 4 em strategy.md");
}

export function rsi(_closes, _period = 14) {
  throw new Error("indicators.js não implementado — ver Passo 4 em strategy.md");
}

export function volumeMA(_volumes, _period = 20) {
  throw new Error("indicators.js não implementado — ver Passo 4 em strategy.md");
}
