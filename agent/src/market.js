/**
 * Conector de dados de mercado — Passo 3 (stub)
 * Usa ccxt para buscar OHLCV. Em paper mode usa dados históricos ou mock.
 */

// TODO Passo 3: instalar ccxt e implementar fetchOHLCV(symbol, timeframe, limit)
export async function fetchOHLCV(_symbol, _timeframe, _limit = 100) {
  throw new Error("market.js não implementado — ver Passo 3 em strategy.md");
}
